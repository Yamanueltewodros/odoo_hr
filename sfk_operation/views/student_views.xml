<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="sfk_student_view_list" model="ir.ui.view">
            <field name="name">sfk.student.list</field>
            <field name="model">sfk.student</field>
            <field name="arch" type="xml">
                <list string="Students"
                      decoration-success="status == 'completed'"
                      decoration-warning="status == 'withdrawn'"
                      decoration-info="status == 'active'">
                    <field name="name"/>
                    <field name="program_id"/>
                    <field name="age"/>
                    <field name="enrollment_date"/>
                    <field name="status" widget="badge"
                           decoration-success="status == 'completed'"
                           decoration-warning="status == 'withdrawn'"
                           decoration-info="status == 'active'"/>
                </list>
            </field>
        </record>

        <record id="sfk_student_view_form" model="ir.ui.view">
            <field name="name">sfk.student.form</field>
            <field name="model">sfk.student</field>
            <field name="arch" type="xml">
                <form string="Student">
                    <header>
                        <button name="action_set_active" string="Set Active"
                                type="object" class="btn-primary"
                                invisible="status == 'active'"/>
                        <button name="action_set_withdrawn" string="Withdraw"
                                type="object"
                                invisible="status == 'withdrawn'"/>
                        <button name="action_set_completed" string="Mark Completed"
                                type="object"
                                invisible="status == 'completed'"/>
                        <field name="status" widget="statusbar"
                               statusbar_visible="active,withdrawn,completed"/>
                    </header>
                    <sheet>
                        <group>
                            <group string="Personal Details">
                                <field name="name"/>
                                <field name="date_of_birth"/>
                                <field name="age" readonly="1"/>
                                <field name="notes"/>
                            </group>
                            <group string="Enrollment Info">
                                <field name="program_id"/>
                                <field name="enrollment_date"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Enrollments">
                                <field name="enrollment_ids"
                                       context="{'default_program_id': program_id}">
                                    <list editable="bottom">
                                        <field name="program_id"/>
                                        <field name="term_id"
                                               domain="[('program_id', '=', program_id)]"/>
                                        <field name="course_id"/>
                                        <field name="enrollment_date"/>
                                        <field name="status"/>
                                    </list>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                    <chatter/>
                </form>
            </field>
        </record>

        <record id="action_sfk_student" model="ir.actions.act_window">
            <field name="name">Students</field>
            <field name="res_model">sfk.student</field>
            <field name="view_mode">list,form</field>
        </record>
    </data>
</odoo>
